import {Capsule} from "./index";
import {createRouting} from "./createRouting";

const {
    Router,
    Linkage,
    routing,
    pathSwitch
} = createRouting(Capsule);


export {
    Router,
    Linkage,
    routing,
    pathSwitch
}